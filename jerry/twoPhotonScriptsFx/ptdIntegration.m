function outputStruct = correctTrials(inputStruct,trials,action)
% outputStruct = ptdIntegration(inputStruct)
% This function 1) uses photodiode info when available or usse LG's script
% to find the correct timing for each trial, then adds the information into
% a new field; 2) deletes manually designated trials if chosen to do so.
% This is also a partial wrapper for the trialDropper function.










end